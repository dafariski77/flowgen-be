services:
  # ─── NestJS Backend API ───
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: flowgen-api
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      PORT: 3000
